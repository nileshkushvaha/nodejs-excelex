node_modules/
dist/
.next/
.turbo/
.env
.env.local
*.log
.DS_Store

# TypeScript incremental build state. Tracking it makes a clean build
# silently emit nothing, because tsc believes the output already exists.
*.tsbuildinfo
.claude/
