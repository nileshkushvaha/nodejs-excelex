import { PlaceholderPage } from "@/components/placeholder-page";

export const metadata = { title: "Branches · ExcelEx" };

export default function BranchesPage() {
  return (
    <PlaceholderPage
      title="Branches"
      phase="Phase 2"
      description="Branches are the unit the branch authorisation scope is built on, so the table exists and is protected already — a branch row cannot be read across clients. The management screens arrive with the master-data module."
    />
  );
}
